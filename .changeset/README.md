# Changesets

Use Changesets to describe user-facing changes in PRs.

Create a changeset:

```bash
bun run changeset
```

Check pending release plan:

```bash
bun run changeset:status
```

Version packages locally (normally done by the bot PR):

```bash
bun run changeset:version
```
